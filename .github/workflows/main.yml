
name: Log on Main Push

on:
  push:
    branches:
      - main

jobs:
  log1:
    runs-on: ubuntu-latest
    steps:
      - name: Log Push Event
        run: echo "Push event detected on the main branch." 
